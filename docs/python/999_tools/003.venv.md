# venv

```sh
python3 -m venv venvCyber     # 创建虚拟环境
source venvCyber/bin/activate # 激活虚拟环境
deactivate # 退出虚拟环境

pip freeze > requirements.txt   # 创建 requirements.txt
pip install -r requirements.txt # 安装依赖，-r requirement
```
