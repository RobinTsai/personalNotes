format: "keywords..."(camelStyle), "usage", "else"
so you can use shell command `cat piecesInProject.txt | grep keywords` to search

"crossDomain ajax CORS", "{crossDomain:true, xhrFields:{withCredentials: true}}"
"CSS border in div", "{border-sizing:border-box;}"
